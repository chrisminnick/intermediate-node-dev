# Test Environment Configuration
NODE_ENV=test
PORT=3001

# Test Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=task_management_test
DB_USER=postgres
DB_PASSWORD=password
DB_MAX_CONNECTIONS=5

# Authentication (use different secret for tests)
JWT_SECRET=test-secret-key-for-testing-only
JWT_EXPIRES_IN=1h

# Disable external services in tests
REDIS_HOST=
EMAIL_HOST=

# Enable test-specific logging
ENABLE_TEST_LOGS=false
LOG_LEVEL=error